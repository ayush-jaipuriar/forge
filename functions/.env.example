# Forge Functions local environment example
#
# Current Phase 4 rule:
# - prefer Firebase-managed runtime configuration for deployed Functions
# - do not place production secrets in this file
# - keep this file placeholder-only unless a future local emulator workflow
#   truly requires explicit environment variables
#
# Firebase-managed values such as FIREBASE_CONFIG are injected by the runtime
# and should not be copied into local files manually.
